const Users = require("../models/user.model");

exports.signIn = async (req, res) => {
  res.json({
    accessToken: "mádbasd",
    name: "manh",
  });
};

exports.signUp = async (req, res) => {
  res.json({
    accessToken: "mádbasd",
    name: "manh",
  });
};
